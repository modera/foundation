
        l10nMomentJs();
        l10nExtJs({
            //currencySign: '\u20ac', // Euro
            dateFormat: '{{ modera_backend_languages_ext_date_format(locale) }}',
            timeFormat: '{{ modera_backend_languages_ext_time_format(locale) }}',
            startDay: {{ modera_backend_languages_ext_start_day(locale) }}
        });
